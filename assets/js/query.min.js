
//
document.onkeydown = function () {
  if (window.event && window.event.keyCode == 123) {
    alert("你最好不要这样做");
    window.event.keyCode = 0;
    window.event.returnValue = false;
  }
  if (window.event && window.event.keyCode == 13) {
    window.event.keyCode = 505;
  }
  if (window.event && window.event.keyCode == 8) {
    alert(str + "\n请使用Del键进行字符的删除操作！");
    window.event.returnValue = false;
  }
}
//
// function stop() { alert("禁止右键，复制粘贴请用Ctrl+A  Ctrl+C  Ctrl+V"); return false; }
// document.oncontextmenu = stop;
//
webdriver = window.navigator.webdriver;
if (webdriver) {
  console.log('are you a SB?')
}
else {
  console.log('Welcome！')
}
//
(() => {
  function block() {
    setInterval(() => {
      debugger;
    }, 50);
  }
  try {
    block();
  } catch (err) { }
})();
//
function play_url() {
  var divId = "https://jx.bozrc.com:4433/player/?url=";
  var format = "";
  var url = document.getElementById('video_url').value;
  if (url != "") {
    var patt1 = /http:\/\/m.le.com/g;   //LS视频
    var patt2 = /https:\/\/m.mgtv.com/g;  //MG视频
    var patt3 = /https:\/\/m.iqiyi.com/g; //AQY视频
    var patt4 = /https:\/\/m.bilibili.com/g;  //BLBL视频
    var patt5 = /https:\/\/m.v.qq.com/g;  //TX视频
    var patt6 = /https:\/\/m.youku.com/g; //YK视频
    var patt7 = /https:\/\/m.pptv.com/g;  //PPTV视频
    if(url.match(patt1) != null){ //LS视频
      url = "http://www.le.com/ptv/vplay/"+url.match(/[0-9]+/g)+".html";
    }
    if (url.match(patt2) != null) { //MG视频
      url = url.replace(patt2, "https://www.mgtv.com");
    }
    if(url.match(patt3) != null){ //AQY视频
      url = url.replace(patt3, "https://www.iqiyi.com");
    }
    if(url.match(patt4) != null){ //BLBL视频
      url = url.replace(patt4, "https://www.bilibili.com");
    }
    if(url.match(patt5) != null){ //TX视频
      // window.alert("不支持手机端的腾讯视频解析！请复制电脑端的链接进行解析");
      // var cid = url.match(/cid=(\S*)&vid=/)[1];
      // var vid = url.match(/vid=(\S*)&ptag/)[1];
      // url = "https://v.qq.com/x/cover/"+cid+"/"+vid+".html";
    }
    if(url.match(patt6) != null){ //YK视频
      // window.alert("不支持手机端的优酷视频解析！请复制电脑端的链接进行解析");
      // var uid = url.match(/video\/(\S*)?source/)[1];
      // url = "https://v.youku.com/v_show/"+uid+".html";
    }
    if(url.match(patt7) != null){ //PPTV视频
      url = url.replace(patt7, "http://v.pptv.com");
    }
    document.getElementById("vip_player").src = divId + url;
    // window.alert(url);
  }
}
//
console.group("网站信息");
console.log("作者：Colyn");
console.log("名称：Colyn's VIP");
console.groupEnd();  